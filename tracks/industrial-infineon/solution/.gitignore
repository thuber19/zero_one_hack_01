# Python / venv
.venv/
__pycache__/
*.pyc
*.egg-info/

# Run artifacts (checkpoints, predictions, plots, tensorboard).
# Ignore bulk output, but DO commit the submission deliverables + scores + logs.
artifacts/*
!artifacts/nextstep.csv
!artifacts/completion.csv
!artifacts/anomaly.csv
!artifacts/metrics.json
!artifacts/metrics_hybrid.json
!artifacts/metrics_per_family.json
!artifacts/per_family_scores.txt
!artifacts/raw/
!artifacts/tb_logs/
runs/
logs/
wandb/

# Build orchestration scratch
build_workflow.js

# Stray MCP / tool databases that land in the cwd
*.rvf
*.rvf.lock
*.db
agentdb.*
ruvector.db
configs/_run_*.yaml

# pitch deck build venv
pitch/.pptxvenv/
